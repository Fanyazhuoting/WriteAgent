global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/prometheus-alerts.yml

scrape_configs:
  - job_name: writeagent
    static_configs:
      - targets:
          - writeagent:8000
    metrics_path: /metrics

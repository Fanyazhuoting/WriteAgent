services:
  writeagent:
    image: ${WRITEAGENT_IMAGE:-ghcr.io/your-org/writeagent}:${WRITEAGENT_TAG:-staging}
    environment:
      PROMPT_VERSION: ${PROMPT_VERSION:-v1}
      LANGCHAIN_TRACING_V2: ${LANGCHAIN_TRACING_V2:-false}
    labels:
      app.environment: staging
